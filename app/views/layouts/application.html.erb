<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if notice %>
      <p class="notice notification mb-0 bg-success text-white p-2 text-center"><%= notice %></p>
    <% end %>

    <% if alert %>
      <p class="alert notification mb-0 bg-danger text-white p-2 text-center"><%= alert %></p>
    <% end %>

    <div class="bg-dark p-2 row mb-3">
      <div class="col"></div>
      <div class="col text-center align-items-center">
        <%= link_to 'Private-Events', '/', class: 'text-white'%>
      </div>
      <div class="col d-flex justify-content-end">
        <% if user_signed_in? %>
          <%= link_to "Sign Out", destroy_user_session_path, :method => :delete %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "p-2" %>
          <%= link_to "Sign up", new_user_registration_path, class: "p-2" %>
        <% end %>
      </div>
    </div>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
